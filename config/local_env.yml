# Each entry gets set as a local environment variable.
# This file overrides ENV variables in the Unix shell.
# Following code makes password available as ENV["POSTGRES_SECRET"]
POSTGRES_SECRET: '1776Washington44*'